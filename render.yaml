services:
  - type: web
    name: campus-connect-backend
    env: node
    buildCommand: cd backend-starter && npm install
    startCommand: cd backend-starter && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5001 