services:
  - type: web
    name: campus-connect-backend
    env: node
    rootDir: backend-starter
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5001